source "https://rubygems.org"

# Jekyll is provided via github-pages in the plugin group.
gem "webrick"

group :jekyll_plugins do
  gem "github-pages"
  gem "jekyll-include-cache"
  gem "jekyll-compose"
end

# Windows timezone data (safe; no native build)
gem "tzinfo-data", platforms: [:windows]